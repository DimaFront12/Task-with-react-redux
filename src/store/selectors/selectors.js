
export const selectId = ({ task }) => task.id

export const selectTitle = ({ task }) => task.title

export const selectTasks = (state) => state.tasks

export const selectValueInput = ({ options }) => options.valueInput

export const selectIsLoading = ({ options }) => options.isLoading

export const selectIsSorted = ({ options }) => options.isSorted

